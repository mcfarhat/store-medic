# Copy to .env and fill in. NEVER commit .env.
PORT=8787
# WooCommerce REST API (read-only keys are enough for watching)
WOO_STORE_URL=https://example-store.com
WOO_CONSUMER_KEY=ck_xxx
WOO_CONSUMER_SECRET=cs_xxx
# Tavily (evidence/citations for verify step) -- free tier ok
TAVILY_API_KEY=tvly-xxx
# LLM for triage/explanations (engine is model-agnostic)
LLM_API_KEY=
